    import XCTest
    @testable import webim_client_library

    final class webim_client_libraryTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(webim_client_library().text, "Hello, World!")
        }
    }
